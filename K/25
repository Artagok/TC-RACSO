input y
{
  if(not mxxstopsininputsteps) {
    if (y != 0 and y != 1)
      output y-2; 	// Dom(p) = Inf - 0
  }			// Im(p)  = Inf + Inf+1
  
  else
    output y;
}

input y
{
  if(not mxxstopsininputsteps) {	// Dom(q) = Inf
    if (y != 0)
      output y;
  }
}
